// import actionTypes from '../actions/actionTypes';

export default function authReducer(state = {}, action) {
  let newState = {};
  switch (action.type) {
    default:
      newState = state;
      break;
  }
  return newState;
}
